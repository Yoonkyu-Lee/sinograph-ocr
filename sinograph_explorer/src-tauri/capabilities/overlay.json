{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "Capability for the screen-capture overlay window",
  "windows": ["capture-overlay"],
  "permissions": [
    "core:default"
  ]
}
